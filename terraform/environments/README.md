# Terraform Environments Directory

Will contain:

- dev/
- prod/

Each environment directory contains its own set of Terraform configuration files and state management to isolate resources between different deployment stages.
